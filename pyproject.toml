[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "whisperlivekit"
version = "0.2.16.dev0"
description = "Real-time speech-to-text with speaker diarization using Whisper"
readme = "README.md"
authors = [
    { name = "Quentin Fuxa" }
]
license = { file = "LICENSE" }
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: 3.15",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Multimedia :: Sound/Audio :: Speech"
]

dependencies = [
    # --- existing core deps ---
    "fastapi",
    "librosa",
    "soundfile",
    "uvicorn",
    "websockets",
    "torchaudio>=2.0.0",
    "torch>=2.0.0",
    "huggingface-hub>=0.25.0",
    "tqdm",
    "tiktoken",
    'triton>=2.0.0; platform_machine == "x86_64" and (sys_platform == "linux" or sys_platform == "linux2")',

    # --- REQUIRED for your current TriviasServer startup path ---
    "faster-whisper",
    "onnxruntime",
    "scipy",  # cross_channel_gate.py (was already a transitive dep via librosa)
    "python-docx",  # gehoorverslag.py -- .docx-generatie voor het rapport van gehoor
    "openai",  # llm_backend.py -- client voor lokale/on-prem OpenAI-compatibele LLM's (Ollama/vLLM/etc), NOOIT de cloud-API; ook al stilzwijgend gebruikt door de bestaande openai-api ASR-backend, nu pas expliciet gedeclareerd

    # --- diarization stack ---
    "pyannote.audio",
    "pyannote.core",
    "pyannote.pipeline",
    "diart",

    # (optional but commonly needed in audio pipelines; keep only if your code imports it)
    # "numpy<2",
]

[project.optional-dependencies]
translation = ["nllw"]
sentence_tokenizer = ["mosestokenizer", "wtpsplit"]
# NLLB-200-vertaling voor het /translate-endpoint (zie whisperlivekit/nllb_backend.py) --
# los van "translation" hierboven (dat is nllw's streaming-wrapper, andere usecase).
# ctranslate2/huggingface-hub zijn al harde dependencies (via faster-whisper resp. hierboven).
nllb_translation = ["transformers"]

[project.urls]
Homepage = "https://github.com/GT-GITH/WhisperLiveKit-Trivias.git"

[project.scripts]
whisperlivekit-server = "whisperlivekit.TriviasServer:main"
wlk = "whisperlivekit.TriviasServer:main"

[tool.setuptools]
packages = [
    "whisperlivekit",
    "whisperlivekit.diarization",
    "whisperlivekit.simul_whisper",
    "whisperlivekit.whisper",
    "whisperlivekit.whisper.assets",
    "whisperlivekit.whisper.normalizers",
    "whisperlivekit.web_trivias",
    "whisperlivekit.local_agreement",
    "whisperlivekit.silero_vad_models"
]

[tool.setuptools.package-data]
"whisperlivekit.web_trivias" = ["*.html", "*.css", "*.js", "src/*.svg"]
"whisperlivekit.whisper.assets" = ["*.tiktoken", "*.npz"]
"whisperlivekit.silero_vad_models" = ["*.jit", "*.onnx"]
